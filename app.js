console.log("Hello World!\n==========\n");

// PROJECT Section
console.log("PROJECT:\n==========\n");

const books = document.getElementsByClassName("book");

class book {
    constructor (Title, Author, read) {
        this.Title = "Title";
        this.Author = "Author";
        this.read = read;
    }
}

const libraries = document.getElementsByClassName("Library");

class Library {
    consturctor (bookCount, books, markRead, addBook, ) {
        this.bookCount = bookCount;
        this.books = "books";
        this.markRead = markRead;
        this.addBook = addBook;
    }
}

function blah (checkBox, id="readLibraryCheckbox") {
    if (bookdId == parmId) {
        change the book read property to true, 
        the `checkbox` checked attribute to true,  
        the `checked` disabled attribute to true
    }else {
        dont change anything
    };

}


